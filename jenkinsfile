pipeline{
agent none
    stages{
        stage ('Build') {
            steps{
                retry(3){
                    echo "retrying 3 times"
                    error "error in retry"    
                }

                echo "I don try"
            }
            
        }
    }
}