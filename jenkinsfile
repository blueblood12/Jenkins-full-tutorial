pipeline{
agent none
    stages{
        stage ('Build') {
            steps{
                retry{
                    echo "retrying 3 times"
                    error "error in retry"    
                }

                echo "I don try"
            }
            
        }
    }
}